# Vue Calendar Component

Vue Calendar - это полнофункциональный календарь с поддержкой локализации, событий, настраиваемых диапазонов дат и анимаций.

## Особенности

- 🌍 Поддержка локализации (по умолчанию: русский, английский, французский, немецкий)
- 📅 Выбор даты
- 🗓️ Навигация по месяцам
- 🛡️ Ограничения по минимальной/максимальной дате
- ⚠️ Отключение определенных дат
- 🎨 Красивые анимации переходов между месяцами
- 📱 Адаптивный дизайн
- 📅 Поддержка событий с всплывающими окнами
- 🖱️ Поддержка мыши и touch-устройств
- 🌐 Доступен онлайн на GitHub Pages

## Демо

Посмотрите живое демо компонента: [https://github.com/proksiks/vue-calendar/](https://github.com/proksiks.vue-calendar/)

## Установка

```bash
# Клонируйте проект
git clone <your-repo-url>
cd vue-calendar

# Установите зависимости
npm install

# Запустите проект
npm run dev
```

## Использование

### Базовое использование

```vue
<template>
  <Calendar v-model="selectedDate" />
</template>

<script setup>
import { ref } from 'vue'
import Calendar from './components/calendar.vue'

const selectedDate = ref('2026-01-15')
</script>
```

### Полное использование с параметрами

```vue
<template>
  <Calendar 
    v-model="selectedDate"
    :locale="locale"
    :first-day-of-week="1"
    :min-date="minDate"
    :max-date="maxDate"
    :disabled-dates="disabledDates"
    :highlight-today="true"
    :events="calendarEvents"
    @month-change="handleMonthChange"
    @day-select="handleDaySelect"
  />
</template>

<script setup>
import { ref } from 'vue'
import Calendar from './components/calendar.vue'

const selectedDate = ref('2026-01-15')
const locale = ref('ru')
const minDate = ref('2026-01-01')
const maxDate = ref('2026-12-31')
const disabledDates = ref(['2026-01-01', '2026-01-07'])

// События для отображения во всплывающих окнах
const calendarEvents = ref([
  {
    date: '2026-01-15',
    title: 'Важная встреча',
    description: 'Встреча с командой по проекту'
  },
  {
    date: '2026-01-20',
    title: 'День рождения',
    description: 'День рождения друга'
  }
])

const handleMonthChange = (payload) => {
  console.log(`Изменен месяц: ${payload.month + 1}/${payload.year}`)
}

const handleDaySelect = (payload) => {
  console.log(`Выбрана дата: ${payload.formatted}`, payload.date)
}
</script>
```

## Свойства (Props)

| Свойство | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `v-model` | String \| Date | - | Выбранная дата |
| `locale` | String | `'ru'` | Локализация ('ru', 'en', 'fr', 'de' или пользовательская) |
| `firstDayOfWeek` | Number | `1` | Первый день недели (0 = воскресенье, 1 = понедельник) |
| `minDate` | String \| Date | - | Минимальная разрешенная дата |
| `maxDate` | String \| Date | - | Максимальная разрешенная дата |
| `disabledDates` | Array | `[]` | Массив дат, которые нельзя выбрать |
| `highlightToday` | Boolean | `true` | Подсветка сегодняшнего дня |
| `events` | Array | `[]` | Массив событий для отображения |

### Структура событий

```javascript
const events = [
  {
    date: '2026-01-15',        // Дата события в формате YYYY-MM-DD
    title: 'Название события', // Заголовок события
    description: 'Описание'    // (опционально) Подробное описание
  }
]
```

## События (Events)

| Событие | Payload | Описание |
|---------|---------|----------|
| `update:modelValue` | String (YYYY-MM-DD) | Выбрана новая дата |
| `month-change` | `{ year: number, month: number }` | Изменен месяц (месяц от 0 до 11) |
| `day-select` | `{ date: Date, formatted: String }` | Выбран день |

## Методы

| Метод | Параметры | Описание |
|-------|-----------|----------|
| `addLocale` | `(code: string, data: object)` | Добавить новую локализацию |
| `goToToday` | - | Перейти к сегодняшней дате |
| `goToDate` | `(date: string \| Date)` | Перейти к указанной дате |
| `prevMonth` | - | Перейти к предыдущему месяцу |
| `nextMonth` | - | Перейти к следующему месяцу |
| `getAvailableLocales` | - | Получить список доступных локализаций |
| `updateDisplayDate` | - | Обновить отображаемую дату |

### Пример использования методов

```vue
<template>
  <div>
    <Calendar ref="calendarRef" v-model="selectedDate" />
    <button @click="goToToday">Сегодня</button>
    <button @click="goToNextMonth">Следующий месяц</button>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import Calendar from './components/calendar.vue'

const calendarRef = ref()
const selectedDate = ref('2026-01-15')

const goToToday = () => {
  calendarRef.value?.goToToday()
}

const goToNextMonth = () => {
  calendarRef.value?.nextMonth()
}
</script>
```

## Добавление пользовательской локализации

```javascript
// Добавление испанской локализации
const success = calendarRef.value?.addLocale('es', {
  months: [
    'Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo', 'Junio',
    'Julio', 'Agosto', 'Septiembre', 'Octubre', 'Noviembre', 'Diciembre'
  ],
  daysShort: ['Dom', 'Lun', 'Mar', 'Mié', 'Jue', 'Vie', 'Sáb'],
  daysLong: ['Domingo', 'Lunes', 'Martes', 'Miércoles', 'Jueves', 'Viernes', 'Sábado']
})
```

## Анимации

Компонент включает плавные анимации:
- При переходе к следующему месяцу: слайд вправо
- При переходе к предыдущему месяцу: слайд влево
- При выборе даты: масштабирование и подсветка
- При наведении на день с событием: плавное появление всплывающего окна с анимацией Vue Transition

## Поддержка событий

Компонент поддерживает отображение событий во всплывающих окнах:
- События отображаются при наведении курсора или касании даты
- Автоматическое позиционирование всплывающего окна внутри области видимости
- Индикатор событий (маленький красный кружок) на датах с событиями
- Поддержка заголовка и описания события

## Responsive дизайн

Компонент адаптируется под разные размеры экранов:
- На мобильных устройствах размер ячеек уменьшается
- Всплывающие окна оптимизированы для мобильных устройств
- Touch-события корректно обрабатываются на мобильных устройствах

## Стилизация

Компонент использует Tailwind CSS для стилизации. Вы можете настроить внешний вид, переопределив CSS-переменные или используя пользовательские классы.

## Совместимость

- Vue 3 (Composition API)
- Все современные браузеры
- Поддержка мобильных устройств